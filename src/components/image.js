import React from 'react';
import App from '../App';
import Task from './Task';

let { StyleSheet } = React;

let styles = StyleSheet.create({
  backgroundImage: {
    flex: 1,
    resizeMode: 'cover',
  }
});

export default Task;